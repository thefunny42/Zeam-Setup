[setup]
find-links =
   http://pypi.python.org/simple/
network-timeout = 0
bin-directory = ${setup:prefix-directory}/bin
download-directory = ${setup:prefix-directory}/download
lib-directory = ${setup:prefix-directory}/lib
log-directory = ${setup:var-directory}/log
var-directory = ${setup:prefix-directory}/var
